# Ignore local credentials and keys
credentials.json
*.key
*.pem

# Python / venv
.venv/
env/
__pycache__/
*.pyc

# Streamlit local secrets (if accidentally created)
.streamlit/secrets.toml
